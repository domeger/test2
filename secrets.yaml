# Each item in the manifest maps an encrypted source file to an unencrypted destination file
# All paths should reflect the container's layout
secretFiles:
 - ["models/multi-class-pg.pkl.bkenc", "models/multi-class-pg.pkl"]
